// tailwind.config.ts
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./src/**/*.{ts,tsx,js,jsx}'], // sesuaikan path dengan project kamu
  theme: {
    extend: {
      colors: {
   

      },
    },
  },
  plugins: [],
};

export default config;
